import styled from "styled-components";
import { hidden, HiddenLabel } from "../../styles/reusable";

const HoneyPot = styled.input`
  ${hidden}
`;
export default {
  HoneyPot,
  HiddenLabel,
};
